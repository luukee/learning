
bracket notation
	var = lastName[0];

multidimensional array
	var myArray = [array[newArray]];

Indexes
	access the data inside arrays;
		var myIndex = newArray[1];

mutable
	can be changed freely - like a number, not a string

scope 
	visibility of variables
global scope
	Variables which are defined outside of a function block