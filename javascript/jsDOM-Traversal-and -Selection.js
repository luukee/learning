// assign variable
let listDiv =
const listDiv =

// go to parent node
li.parentNode;

// remove child
ul.removeChild(li);

// select element
document.querySelector('input');

// style an element
listDiv.style.display = 'block';

// change text content
listDiv.textContent("say this");

// select all of a specific element
let li = listUl.querySelectorAll('li');
// select the 2nd of a specific element
let li = listUl.querySelectorAll('li')[3];

// look at an element's previous sibling
li.previousSibling;
// better way of traversing to the previous sibling (always returns a DOM element).
li.previousElementSibling;

// get element by class
event.target.className == 'remove';

// insert before (elementToRemove, elementRefrence)
li.insertBefore(li, prevLi);

// next element sibling
listDiv.nextElementSibling;

// create button
let up = document.createElement("button");
// give it a class
up.className = "up";
//give it text content
up.textContent = "Going Up";

// select first child
ParentNode.firstElementChild;

// select last child
ParentNode.lastElementChild;

// select the children of an element
ParentNode.children;







